{
  "name": "payment-gateway-backend",
  "version": "1.0.0",
  "description": "Payment Gateway API Backend",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "health": "node -e \"require('./src/config/database').pool.query('SELECT 1').then(() => { console.log('✓ database healthy'); process.exit(0); }).catch(e => { console.error('✗ database unhealthy:', e.message); process.exit(1); });\"",
    "seed": "node src/config/database.js",
    "test": "jest src/tests --runInBand"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.0.0",
    "supertest": "^6.3.3"
  }
}
